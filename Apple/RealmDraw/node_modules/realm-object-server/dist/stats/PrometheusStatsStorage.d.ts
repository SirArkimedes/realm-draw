import { StatsLabels } from "./StatsLabels";
import { StatsStorage } from "./StatsStorage";
export interface PrometheusStatsStorageConfig {
    url: string;
}
export declare class PrometheusStatsStorage implements StatsStorage {
    private readonly prometheusUrl;
    constructor(config: PrometheusStatsStorageConfig);
    getInstantValues(name: string, labels?: StatsLabels): Promise<any>;
    private generateQuery(name, labels?);
}
