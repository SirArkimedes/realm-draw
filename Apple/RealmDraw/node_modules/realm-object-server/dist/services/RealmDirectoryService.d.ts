export declare class RealmDirectoryService {
    private logger;
    private adminToken;
    private protectedRealmPaths;
    private discovery;
    private adminRealm;
    private authorizationHeaderName;
    private setLogger(l);
    private findByPath(realmPath, req, shouldCreateString, realmTypeString?, ownerId?);
    private remove(realmPath, req);
    private changeType(realmPath, realmTypeString, req);
    private compact(req);
    private start(server);
    private stop();
    private findSyncLabel();
    private findRealmFile(realmPath);
    private findOrCreateRealmFile(realmPath, getOwner, realmType, syncLabel?);
}
