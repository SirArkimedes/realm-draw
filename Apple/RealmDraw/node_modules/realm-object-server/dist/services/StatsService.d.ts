/// <reference types="express" />
import * as express from "express";
import { StatsStorage, StatsLabels } from "../stats";
export declare type IInstantValuesResponse = {
    [metricName: string]: {
        labels: StatsLabels;
        value: number;
    }[];
};
export declare class StatsService {
    statsStorage: StatsStorage;
    private server;
    protected getExtraLabels(req: express.Request): StatsLabels;
    private start(server);
    private getInstantValues(metricNamesString, req);
    private reportRealmStateSize(path?);
    private sanityCheck();
}
