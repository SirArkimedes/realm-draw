import { IService } from "../Service";
export interface IHealthServiceConfig {
    ignoreHttpTraffic?: boolean;
}
export declare class HealthService implements IService {
    private readonly ignoreHttpTraffic?;
    private started;
    private healthClient;
    private version;
    constructor(config?: IHealthServiceConfig);
    private start(server);
    private live();
    private getHealth(req, res, thisInstance?);
}
